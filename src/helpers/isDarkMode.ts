/**
 * @param {string} displayMode A string that will say dark or light
 * @returns {boolean} Returns true if display mode is dark, else false
 */
export const isDarkMode = (displayMode: string): boolean =>
  displayMode === 'dark';
